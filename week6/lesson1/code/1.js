// let age = +(prompt('Enter your age: '));

let age = 18;

// ___________.________
// Блок условного оператора
if (age < 18) { // Если сравнение окажется верным, то // true
    // Мы входим в этот блок кода
    console.log('Too young!');
} else { // Иначе = false
    // nigation
    // age >= 18
    // Если if не true, то мы входим сюда
    console.log('Buy it!');
}